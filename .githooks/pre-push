#!/usr/bin/env bash
set -euo pipefail

python3 scripts/check_privacy.py

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "Falta gitleaks: instálalo antes de publicar cambios." >&2
  exit 1
fi

gitleaks git . --redact=100 --no-banner
